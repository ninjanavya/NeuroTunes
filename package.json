{
  "name": "neurotunes-workspace",
  "version": "1.0.0",
  "description": "NeuroTunes Workspace - AI-Powered Emotion-Adaptive Music Platform",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend-node && npm install --prefix frontend",
    "dev": "concurrently --kill-others \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ml\"",
    "dev:backend": "npm run dev --prefix backend-node",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:ml": "cd ml-service && ..\\.venv\\Scripts\\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
